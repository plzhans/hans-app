{
  "ref": "force-https",
  "description": "[force-https] 평문 HTTP 요청을 같은 주소의 HTTPS 로 301 - http://medifinder.kr/ 이 리다이렉트 없이 200 을 주고 있었다(SEO 감사에서 발견). 대시보드의 SSL/TLS > Edge Certificates > Always Use HTTPS 토글과 같은 일을 하지만, 레포에 남겨 두려고 규칙으로 건다. 둘 다 켜도 충돌하지 않는다.",
  "expression": "(not ssl)",
  "action": "redirect",
  "action_parameters": {
    "from_value": {
      "target_url": {
        "expression": "concat(\"https://\", http.host, http.request.uri.path)"
      },
      "status_code": 301,
      "preserve_query_string": true
    }
  }
}
