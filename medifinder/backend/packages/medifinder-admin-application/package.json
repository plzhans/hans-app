{
  "name": "@medifinder/admin-application",
  "version": "0.0.1",
  "private": true,
  "description": "MediFinder 응용 계층. hans-api 를 공개 SDK 로만 읽는다 — @hansapp/* 에 의존하지 않는다.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint \"src/**/*.ts\" --fix"
  },
  "dependencies": {
    "@hans-api/sdk": "link:../../../../clients/hans-api-sdk",
    "@nestjs/common": "^11.1.27",
    "reflect-metadata": "^0.2.2"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "typescript": "^5.9.3"
  }
}
