{
  "name": "hansapp-frontend",
  "version": "0.9.1",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n docs,auth,console,admin,medifinder -c cyan,magenta,blue,green,yellow \"pnpm -C hansapp-docs docs:dev\" \"pnpm -C hansapp-auth local\" \"pnpm -C hansapp-web local\" \"pnpm -C hansapp-admin local\" \"pnpm -C medifinder-web local\"",
    "dev:web": "concurrently -k -n auth,console -c magenta,blue \"pnpm -C hansapp-auth local\" \"pnpm -C hansapp-web local\"",
    "dev:admin": "pnpm -C hansapp-admin local"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
