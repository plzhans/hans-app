# 프론트 env 는 커밋한다. 값이 전부 VITE_*(API base·ncloud client id·site url 같은
# 공개 상수)라 클라이언트 번들에 구워져 브라우저에 그대로 노출된다 — GitHub Secrets 로
# 숨길 이유가 없다. 개인 개발용 오버라이드(.env.local, .env.<mode>.local)만 무시한다.
.env*.local
