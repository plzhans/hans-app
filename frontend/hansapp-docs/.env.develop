# hansapp-docs: develop
# Google Analytics 측정 ID. dev 는 기본 미설정(추적 안 함). 필요하면 별도 dev 속성 ID 를 넣는다.
VITE_GOOGLE_ANALYTICS_ID=

VITE_SENTRY_DSN=https://6d76be671ad5b2e2e79ff772b4958f49@o4507886343159808.ingest.de.sentry.io/4511828921614416

# ⚠️ vite 는 이 파일을 스스로 읽지 않는다(mode 가 항상 production). ci-build.sh 가 process.env
# 로 실어 보내 덮는 구조라, **키가 없으면 .env.production 의 운영 값이 이긴다.**

# 문서·포털 주소. canonical·sitemap·og:image 와 상단 HOME 링크가 이 값으로 만들어진다.
# **VITE_ 접두를 붙이지 않는다** — 브라우저 번들이 아니라 빌드(.vitepress/config.ts)가 쓴다.
# 배포 환경은 문서가 포털의 /docs 밑이라 두 값이 같다. 로컬만 갈린다(문서 5272, 포털 5274).
DOCS_ORIGIN=https://develop.plzhans.com
PORTAL_ORIGIN=https://develop.plzhans.com
