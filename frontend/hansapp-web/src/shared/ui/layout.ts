/**
 * 페이지 본문 컨테이너.
 *
 * 헤더·본문·푸터가 같은 값을 써야 화면 양끝의 세로선이 맞는다 — 한 곳만 바꾸면
 * 로고가 본문보다 바깥으로 튀어나온다.
 *
 * 폭은 데스크톱 기준(1280px)이다. 앱 관리는 사무실 모니터에서 다루는 화면이라
 * 표에 열을 더 세우고 좌우로 카드를 나란히 놓을 자리가 필요하다.
 */
export const PAGE_CONTAINER = 'mx-auto w-full max-w-7xl px-4 sm:px-6 lg:px-8';
