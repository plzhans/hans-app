# ⚠️ 이 파일은 **로컬 전용이 아니다.**
#
# vite 는 .env.local 을 **모든 mode 에서** 읽는다(공식 스펙). 이 레포는 파일 이름이 겹치는
# 바람에 이것을 `local` 환경의 설정으로 쓰고 있을 뿐이라, 여기 적은 키는 develop·production
# 빌드에도 그대로 딸려 간다.
#
# **backend 의 local 과 의미가 정반대다.** 백엔드는 config/config.<환경>.yaml 중 **그 환경
# 것 하나만** 골라 읽으므로 config.local.yaml 은 진짜 로컬 전용이고, 운영에는 존재조차
# 하지 않는다. 여기서는 `.local` 이 "나만 쓰는 값" 이 아니라 "**모두에게 딸려가는 값**" 이다.
# (백엔드에서 그 뜻에 해당하는 것은 config/.env.<환경>.local 쪽 개인 오버라이드다.)
#
#   .env  <  .env.local  <  .env.[mode]  <  .env.[mode].local
#             ↑ 항상 읽힘   ↑ 같은 키가 여기 있어야 이긴다
#
# **여기에 키를 추가하면 .env.develop·.env.production 에도 반드시 적을 것.** 값이 달라도 되고
# 비워도 되지만, 없으면 이 파일의 로컬 값이 배포 번들에 박힌다. 빌드는 성공하고 배포도
# 초록불이라 사용자가 눌러봐야 드러난다 — 실제로 .env.production 에 VITE_AUTH_WEB_URL 이
# 없어서 이 파일의 `/auth` 가 이겼고, 운영 로그인이 plzhans.com/auth/login 으로 갔다.
# (ci-build.sh 가 이 규칙을 검사해 어기면 빌드를 세운다.)

# hansapp-web(콘솔): local — 단일 오리진 front door(127.0.0.1:5274). API 도 127.0.0.1 로 맞춘다
# (쿠키는 호스트 기준 → 포털웹·API·인증웹 전부 127.0.0.1 이어야 refresh 쿠키가 공유된다. localhost 금지).
VITE_HANSAPP_BASE_URL=http://127.0.0.1:3000
# 인증웹은 같은 오리진의 /auth 로 프록시된다(vite proxy). 상대경로.
VITE_AUTH_WEB_URL=/auth
# 서비스 루트 도메인. 백엔드 APP_ROOT_DOMAIN·인증웹과 **같은 값**이어야 한다.
# 로그인 힌트 쿠키를 지울 때 심을 때와 같은 domain 을 써야 브라우저가 지운다.
VITE_APP_ROOT_DOMAIN=
# 이 콘솔의 공개 주소. canonical·og:url·JSON-LD 에 박힌다. 끝에 / 를 붙이지 않는다.
VITE_SITE_URL=http://127.0.0.1:5274
# 로그인 힌트 쿠키 이름. 백엔드 auth.cookiePrefix + hansapp.session 과 **같아야** 한다.
# 환경마다 다른 이유: develop 과 운영이 쿠키 도메인(plzhans.com)을 공유해서, 이름까지
# 같으면 서로의 세션을 덮어쓰고 상대 DB 에 없는 토큰이라 거절돼 양쪽이 로그아웃된다.
VITE_SESSION_HINT_COOKIE_NAME=local.hansapp.session
# Google Analytics 측정 ID. 로컬은 미설정(추적 안 함).
VITE_GOOGLE_ANALYTICS_ID=
# 실행 환경 이름(Sentry environment 태그).
VITE_APP_ENV=local
# 에러 추적(Sentry). **로컬은 끈다** — 내 머신 에러가 팀 이슈 스트림에 섞이면 안 된다.
# 연동을 직접 확인하고 싶을 때만 아래 줄의 주석을 푼다(DSN 은 비밀 아님 — 전송 전용 공개값).
VITE_SENTRY_DSN=
#VITE_SENTRY_DSN=https://8109761cd7f3557dc29ff2983298eef0@o4507886343159808.ingest.de.sentry.io/4511811436806224
VITE_SENTRY_TRACES_SAMPLE_RATE=1.0

# 상단·본문의 바로가기 링크. **환경마다 주소가 다르므로 전부 적는다** — 안 적으면
# 코드의 기본값으로 떨어져서 develop 콘솔이 엉뚱한 문서·사이트로 보낸다.
# 로컬만 docs 가 상대경로다 — 이 콘솔(5274)이 /docs 를 문서 dev 서버(5272)로 프록시한다.
VITE_LINK_DOCS=/docs
VITE_LINK_MEDIFINDER=http://127.0.0.1:5173

# 노출용 대표 이메일(푸터 등). 빌드 시 번들에 인라인된다.
# **약관·방침의 연락처는 이 값이 아니라 문서(@hansapp/legal)에 박혀 있다** — 문서에 적힌
# 연락처를 바꾸는 것은 개정이라 공지 대상이고, 환경마다 달라져서도 안 된다. 여기 값은
# 화면 장식용이므로 둘이 어긋나지 않게 같이 고칠 것.
VITE_CONTACT_EMAIL=plzhans@gmail.com

# AI/LLM 업체 키(BYOK) 등록 기능. **`true` 만 켠 것으로 본다** — 빈 값·미설정·`false` 는 전부 꺼짐.
# 아직 열지 않은 기능의 화면을 잠그는 스위치다. 켜면 앱 상세의 AI/LLM 탭에서 등록·수정·삭제가
# 열리고, 꺼 두면 버튼을 누를 때 "사용이 제한되어 있습니다" 안내만 뜬다.
# **서버는 이 값을 모른다** — 백엔드 API 는 언제나 열려 있고 잠그는 것은 화면뿐이다.
VITE_FEATURE_LLM_PROVIDER_KEYS=true
