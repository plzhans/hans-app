# hansapp-web(콘솔): develop
VITE_HANSAPP_BASE_URL=https://develop-api.plzhans.com
# 로그인 포털(hans-auth) dev URL. 프론트 dev 도메인 확정 시 채운다.
# 로그인 포털. **빈 값이라도 반드시 적는다** — 안 적으면 .env.local 의 /auth 가 새어
# 들어온다(vite 는 .env.local 을 모든 mode 에서 읽는다).
VITE_AUTH_WEB_URL=https://develop-auth.plzhans.com
# Google Analytics 측정 ID. dev 는 기본 미설정(추적 안 함). 필요하면 별도 dev 속성 ID 를 넣는다.
VITE_GOOGLE_ANALYTICS_ID=
# 실행 환경 이름. Sentry 의 environment 태그가 이 값이다(백엔드 APP_ENV 와 이름을 맞춘다).
# vite 의 mode 로는 못 가른다 — build 는 develop/production 둘 다 mode=production 이다.
VITE_APP_ENV=develop
# 에러 추적(Sentry). **DSN 은 비밀이 아니다** — 이벤트 전송 전용 공개 엔드포인트라 어차피
# 클라이언트 번들에 그대로 구워진다. 비우면 Sentry 를 켜지 않는다.
VITE_SENTRY_DSN=https://8109761cd7f3557dc29ff2983298eef0@o4507886343159808.ingest.de.sentry.io/4511811436806224
# 성능 트레이스 표본 비율(0=끔, 1=전부). dev 는 트래픽이 적어 전부 본다.
VITE_SENTRY_TRACES_SAMPLE_RATE=1.0
