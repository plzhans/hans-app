node_modules/
dist/
*.log
.DS_Store

# 이 프론트의 .env.<환경> 은 커밋한다. 값이 전부 VITE_*(클라이언트 번들에 구워져 브라우저에
# 다 노출되는 공개 상수: API base·ncloud client id·site url)라 GitHub Secrets 로 숨길 이유가
# 없다. 루트 .gitignore 의 **/.env.* 를 여기서 되돌린다. .env.local(개인 개발용)만 계속 무시.
!.env.develop
!.env.production
