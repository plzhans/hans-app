/**
 * Generated by orval v8.20.0 🍺
 * Do not edit manually.
 * Hans API
 * Hans API backend 문서
 * OpenAPI spec version: 0.13.0
 */
import type { HiraAssessmentGrade } from './hiraAssessmentGrade';

/**
 * 기관 하나의 평가등급. 필드는 가이드가 아니라 실응답을 따랐다(표본 1,000건).
 *
 * **asmGrd02·asmGrd11 은 존재하지 않는다** — 원본에 없다(폐지 항목으로 보인다). 번호가 연속이 아니다.
 *
 * 평가대상은 36,599개 기관이고 대부분 의원(clCd=31)이다. 병원급 전용이 아니다.
 */
export interface HiraHospitalAssessmentItem {
  /** 암호화된 요양기호 */
  ykiho?: string;
  /** 요양기관명 */
  yadmNm?: string;
  /** 종별코드. **숫자와 문자열이 섞여 온다.** '01'(상급종합)만 문자열이고 나머지(11·21·28·29·31·71·72·75…)는 정수다 — 앞자리 0 이 있는 코드만 문자열로 남기 때문이다. 비교할 때는 문자열로 맞춘다. */
  clCd?: number | string;
  /** 종별코드명 */
  clCdNm?: string;
  /** 주소 */
  addr?: string;
  /** 급성기뇌졸중 */
  asmGrd01?: HiraAssessmentGrade;
  /** 혈액투석 */
  asmGrd03?: HiraAssessmentGrade;
  /** 의료급여정신과 */
  asmGrd04?: HiraAssessmentGrade;
  /** 수술부위 감염예방 항생제 */
  asmGrd05?: HiraAssessmentGrade;
  /** 관상동맥우회술 */
  asmGrd06?: HiraAssessmentGrade;
  /** 급성상기도감염 항생제 처방률 */
  asmGrd07?: HiraAssessmentGrade;
  /** 주사제 처방률 */
  asmGrd08?: HiraAssessmentGrade;
  /** 약품목수 */
  asmGrd09?: HiraAssessmentGrade;
  /** 요양병원 */
  asmGrd10?: HiraAssessmentGrade;
  /** 대장암 */
  asmGrd12?: HiraAssessmentGrade;
  /** 위암 */
  asmGrd13?: HiraAssessmentGrade;
  /** 유방암 */
  asmGrd14?: HiraAssessmentGrade;
  /** 폐암 */
  asmGrd15?: HiraAssessmentGrade;
  /**
     * 천식. **이 항목만 등급 표기가 다르다. 등급 체계 자체는 나머지와 같다.**
     *
     *  일반 21개 : 1 2 3 4 5 '등급제외'
     *  천식 : '양호' 2 3 4 5 0
     *
     * 1등급을 '양호' 로, 등급대상 제외를 0 으로 표기할 뿐이다. 그래서 천식에는 1 과 '등급제외' 가 오지 않는다. 가이드가 이 항목만 '(양호한 기관 공개)' 로 표기한 것이 이 뜻이다(나머지 21개는 '(1~5등급, 등급제외)').
     *
     * **0 은 최하 등급도 값 없음도 아니다.** 0 은 등급제외이고 최하는 5 다. 다른 항목과 비교하려면 '양호'→1, 0→'등급제외' 로 옮겨서 쓴다.
     */
  asmGrd16?: number | string;
  /** 만성폐쇄성폐질환 */
  asmGrd17?: HiraAssessmentGrade;
  /** 폐렴 */
  asmGrd18?: HiraAssessmentGrade;
  /** 중환자실 */
  asmGrd19?: HiraAssessmentGrade;
  /** 신생아중환자실 */
  asmGrd20?: HiraAssessmentGrade;
  /** 마취 */
  asmGrd21?: HiraAssessmentGrade;
  /** 정신건강 입원영역 */
  asmGrd22?: HiraAssessmentGrade;
  /** 급성하기도감염 항생제 처방률 */
  asmGrd23?: HiraAssessmentGrade;
  /** 고혈압·당뇨병 */
  asmGrd24?: HiraAssessmentGrade;
}
