/**
 * Generated by orval v8.20.0 🍺
 * Do not edit manually.
 * Hans API
 * Hans API backend 문서
 * OpenAPI spec version: 0.13.0
 */
import type { HiraApplyDate } from './hiraApplyDate';
import type { HiraClassCode } from './hiraClassCode';
import type { HiraNonPaymentCode } from './hiraNonPaymentCode';

/**
 * 기관 하나의 비급여 항목 한 건. **(기관, 항목) 한 쌍이 한 행**이라 `ykiho` 하나에 수백 행이 온다(중앙대학교병원 666행).
 *
 * 요약(List2)과 달리 가격이 범위가 아니라 **실제 청구금액 `curAmt` 단일값**이고, 대신 분류체계(중/소/상세분류) 필드가 없다.
 */
export interface HiraNonPaymentDetailItem {
  /** 암호화된 요양기호 */
  ykiho?: string;
  /** 병원명 */
  yadmNm?: string;
  clCd?: HiraClassCode;
  /** 종별코드명 (예: 종합병원) */
  clCdNm?: string;
  /** 시도코드 */
  sidoCd?: number;
  /** 시도명 (예: 서울) */
  sidoCdNm?: string;
  /** 시군구코드 */
  sgguCd?: number;
  /** 시군구명 (예: 영등포구) */
  sgguCdNm?: string;
  /** 병원 비급여 안내 URL. **없는 행이 있다**(표본 4,000행 중 391건). */
  urlAddr?: string;
  /** 일련번호 */
  sno?: number;
  npayCd?: HiraNonPaymentCode;
  /** 비급여 한글명. '대분류/중분류/소분류' 를 슬래시로 이어 붙인 형태다 (예: MRI진단료/근골격계/고관절). */
  npayKorNm?: string;
  /** 요양기관이 자체적으로 붙인 항목명 (예: 'Hip MRI', 'MegaDerm 3~4mm*1*1Cm (엘앤씨바이오)'). **표기가 기관 제각각**이라 이 값으로 기관 간 비교에 쓰지 않는다 — 비교는 `npayCd` 로 한다. */
  yadmNpayCdNm?: string;
  /** 적용개시일자 (YYYYMMDD, JSON number) */
  adtFrDd?: HiraApplyDate;
  /** 적용종료일자 (YYYYMMDD, JSON number). **종료가 없으면 `99991231`** 이다 — 현재 유효한 가격이라는 뜻이지 실제 날짜가 아니다. */
  adtEndDd?: HiraApplyDate;
  /** 현재금액 (단위: 원). 그 기관이 실제로 받는 금액이다. */
  curAmt?: number;
}
