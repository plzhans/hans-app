/**
 * Generated by orval v8.20.0 🍺
 * Do not edit manually.
 * Hans API
 * Hans API backend 문서
 * OpenAPI spec version: 0.13.0
 */
import type { AiSearchHistoryTurnDto } from './aiSearchHistoryTurnDto';
import type { AiSearchParamsDto } from './aiSearchParamsDto';

export interface AiSearchRequestDto {
  /**
     * 자연어 질문
     * @maxLength 300
     */
  q: string;
  /**
     * 직전 응답의 `params` 를 그대로 돌려보낸다. 앞서 잡힌 조건을 이어받는 수단이다 — 서버는 대화를 기억하지 않는다.
     *
     * 새 주제를 시작하려면 빼고 보낸다. `answer`·`reason` 은 보내도 무시한다.
     */
  context?: AiSearchParamsDto;
  /**
     * 앞서 오간 말. "아까 말한 증상" 처럼 앞을 가리키는 말을 푸는 데 쓴다 (`context` 는 조건만 담아 이것까지는 못 푼다).
     *
     * `answer` 를 보낼 때는 그 응답의 `answerSignature` 를 `signature` 로 같이 실어야 한다. 없거나 맞지 않으면 그 턴의 `answer` 만 버리고 `question` 은 그대로 쓴다.
     *
     * **최근 3마디만 쓴다**(더 보내면 앞쪽부터 버린다). 한 마디의 길이도 자른다 — 질문 300자, 답 800자.
     */
  history?: AiSearchHistoryTurnDto[];
}
