/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * 건강보험심사평가원(HIRA) 비급여진료비정보조회서비스
 * 병원급 이상 의료기관의 비급여 진료비(가격) 정보. 의료법에 따라 공개되는 자료다.

**의원(clCd=31)은 없다**(2026-07 실측). 표본에서 나온 종별은 상급종합(01)·종합병원(11)·병원(21)·요양병원(28)·정신병원(29)·치과병원(41)·한방병원(92) 뿐이다. 병원 79,739개 중 대부분이 의원이므로 **이 데이터가 붙는 기관은 소수다.**

두 오퍼레이션의 성격이 반대다 — 섞어 쓰지 마라:

| | 상세(Dtl) | 요약(List2) |
| --- | --- | --- |
| 한 행 | 기관 하나의 **실제 청구금액**(`curAmt`) | 기관 하나의 항목별 **가격 범위**(`minPrc`~`maxPrc`) |
| 병원별 조회 | **`ykiho` 로 된다** | **`ykiho` 가 무시된다** |
| 항목별 조회 | **안 된다**(`npayCd`·`itemCd` 무시) | `itemCd` 로 된다 |
| 분류 코드 | 없음 | 중/소/상세분류 다 있음 |
| 전건 | 259,353 | 187,627 |

**병원 상세 화면에 붙일 거라면 상세(Dtl) + `ykiho` 다** — 병원당 1콜로 그 기관의 비급여 전 항목이 나온다(예: 중앙대학교병원 666건).

가이드가 `ykiho`·`itemCd` 를 '필수'라 적어 놨지만 **둘 다 옵션이다**(2026-07 실측). 생략하면 전 기관을 페이지 단위로 받는다.

이 서비스에는 비급여항목코드정보·종별정보·지역별정보 오퍼레이션도 있지만, 여기엔 위 2종만 정제해 두었다.
 * OpenAPI spec version: 1.0.0
 */
import type {
  GetNonPaymentItemHospDetailListParams,
  GetNonPaymentItemHospSummaryListParams,
  NonPaymentDetailResponse,
  NonPaymentSummaryResponse,
} from './model';

import { krDataMutator } from '../../mutator';

/**
 * 기관별 비급여 항목의 **실제 청구금액**(`curAmt`) 목록. 16.3월 이후 자료.

**`ykiho` 는 가이드와 달리 옵션이다**(2026-07 실측). 주면 그 기관만(중앙대학교병원 666건), 생략하면 전 기관 259,353건을 페이지 단위로 받는다.

**항목으로는 못 거른다** — `npayCd`·`itemCd` 를 넘겨도 무시되고 전건이 나온다(2026-07 실측). 특정 항목만 필요하면 기관 단위로 받아서 호출부에서 걸러라. 지역·종별·병원명 필터는 정상 동작한다.
 * @summary 비급여항목병원목록상세 (16.3월 이후)
 */
export type getNonPaymentItemHospDetailListResponse200 = {
  data: NonPaymentDetailResponse;
  status: 200;
};

export type getNonPaymentItemHospDetailListResponseSuccess =
  getNonPaymentItemHospDetailListResponse200 & {
    headers: Headers;
  };
export type getNonPaymentItemHospDetailListResponse =
  getNonPaymentItemHospDetailListResponseSuccess;

export const getGetNonPaymentItemHospDetailListUrl = (
  params?: GetNonPaymentItemHospDetailListParams,
) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/nonPaymentDamtInfoService/getNonPaymentItemHospDtlList?${stringifiedParams}`
    : `/nonPaymentDamtInfoService/getNonPaymentItemHospDtlList`;
};

export const getNonPaymentItemHospDetailList = async (
  params?: GetNonPaymentItemHospDetailListParams,
  options?: RequestInit,
): Promise<getNonPaymentItemHospDetailListResponse> => {
  return krDataMutator<getNonPaymentItemHospDetailListResponse>(
    getGetNonPaymentItemHospDetailListUrl(params),
    {
      ...options,
      method: 'GET',
    },
  );
};

/**
 * 기관별 비급여 항목의 **가격 범위**(`minPrc`~`maxPrc`)와 분류체계(중/소/상세분류) 목록. 16.3월 이후 자료.

**`itemCd` 는 가이드와 달리 옵션이다**(2026-07 실측). 생략하면 전건 187,627건이다.

**`ykiho` 는 먹지 않는다** — 넘겨도 무시된다(2026-07 실측). 병원 단위로 보려면 `yadmNm` 을 쓰거나 상세(Dtl)를 써라.
 * @summary 비급여항목병원목록요약 (16.3월 이후)
 */
export type getNonPaymentItemHospSummaryListResponse200 = {
  data: NonPaymentSummaryResponse;
  status: 200;
};

export type getNonPaymentItemHospSummaryListResponseSuccess =
  getNonPaymentItemHospSummaryListResponse200 & {
    headers: Headers;
  };
export type getNonPaymentItemHospSummaryListResponse =
  getNonPaymentItemHospSummaryListResponseSuccess;

export const getGetNonPaymentItemHospSummaryListUrl = (
  params?: GetNonPaymentItemHospSummaryListParams,
) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/nonPaymentDamtInfoService/getNonPaymentItemHospList2?${stringifiedParams}`
    : `/nonPaymentDamtInfoService/getNonPaymentItemHospList2`;
};

export const getNonPaymentItemHospSummaryList = async (
  params?: GetNonPaymentItemHospSummaryListParams,
  options?: RequestInit,
): Promise<getNonPaymentItemHospSummaryListResponse> => {
  return krDataMutator<getNonPaymentItemHospSummaryListResponse>(
    getGetNonPaymentItemHospSummaryListUrl(params),
    {
      ...options,
      method: 'GET',
    },
  );
};
