/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * 건강보험심사평가원(HIRA) 비급여진료비정보조회서비스
 * 병원급 이상 의료기관의 비급여 진료비(가격) 정보. 의료법에 따라 공개되는 자료다.

**의원(clCd=31)은 없다**(2026-07 실측). 표본에서 나온 종별은 상급종합(01)·종합병원(11)·병원(21)·요양병원(28)·정신병원(29)·치과병원(41)·한방병원(92) 뿐이다. 병원 79,739개 중 대부분이 의원이므로 **이 데이터가 붙는 기관은 소수다.**

두 오퍼레이션의 성격이 반대다 — 섞어 쓰지 마라:

| | 상세(Dtl) | 요약(List2) |
| --- | --- | --- |
| 한 행 | 기관 하나의 **실제 청구금액**(`curAmt`) | 기관 하나의 항목별 **가격 범위**(`minPrc`~`maxPrc`) |
| 병원별 조회 | **`ykiho` 로 된다** | **`ykiho` 가 무시된다** |
| 항목별 조회 | **안 된다**(`npayCd`·`itemCd` 무시) | `itemCd` 로 된다 |
| 분류 코드 | 없음 | 중/소/상세분류 다 있음 |
| 전건 | 259,353 | 187,627 |

**병원 상세 화면에 붙일 거라면 상세(Dtl) + `ykiho` 다** — 병원당 1콜로 그 기관의 비급여 전 항목이 나온다(예: 중앙대학교병원 666건).

가이드가 `ykiho`·`itemCd` 를 '필수'라 적어 놨지만 **둘 다 옵션이다**(2026-07 실측). 생략하면 전 기관을 페이지 단위로 받는다.

이 서비스에는 비급여항목코드정보·종별정보·지역별정보 오퍼레이션도 있지만, 여기엔 위 2종만 정제해 두었다.
 * OpenAPI spec version: 1.0.0
 */
import type { PageNoParameter } from './pageNoParameter';
import type { NumOfRowsParameter } from './numOfRowsParameter';
import type { ItemCdParameter } from './itemCdParameter';
import type { ClCdParameter } from './clCdParameter';
import type { SidoCdParameter } from './sidoCdParameter';
import type { SgguCdParameter } from './sgguCdParameter';
import type { YadmNmParameter } from './yadmNmParameter';
import type { SearchWrdParameter } from './searchWrdParameter';

export type GetNonPaymentItemHospSummaryListParams = {
  /**
   * 페이지 번호
   */
  pageNo?: PageNoParameter;
  /**
   * 한 페이지 결과 수. 생략하면 10. 1000 까지 정상 동작한다(2026-07 실측).
   */
  numOfRows?: NumOfRowsParameter;
  /**
   * 비급여 항목코드 (예: ABZ010001). **가이드는 필수라 하지만 옵션이다**(2026-07 실측). 요약(List2) 전용 — 상세(Dtl)에서는 무시된다.
   */
  itemCd?: ItemCdParameter;
  /**
   * 의료기관 종별코드. **앞자리 0 을 살린 문자열로 넘겨라**(예: '01' 상급종합). 병원급 이상만 있어 31(의원)은 결과가 없다.
   */
  clCd?: ClCdParameter;
  /**
   * 시도코드 (예: 110000 서울)
   */
  sidoCd?: SidoCdParameter;
  /**
   * 시군구코드 (예: 110013 영등포구)
   */
  sgguCd?: SgguCdParameter;
  /**
   * 병원명 (예: 중앙대학교병원). 상세·요약 둘 다 동작한다.
   */
  yadmNm?: YadmNmParameter;
  /**
   * 비급여항목명 검색어 (예: 인실). 요약(List2) 전용.
   */
  searchWrd?: SearchWrdParameter;
};
