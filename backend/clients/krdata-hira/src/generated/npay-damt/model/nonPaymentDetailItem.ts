/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * 건강보험심사평가원(HIRA) 비급여진료비정보조회서비스
 * 병원급 이상 의료기관의 비급여 진료비(가격) 정보. 의료법에 따라 공개되는 자료다.

**의원(clCd=31)은 없다**. 표본에서 나온 종별은 상급종합(01)·종합병원(11)·병원(21)·요양병원(28)·정신병원(29)·치과병원(41)·한방병원(92) 뿐이다. 병원 79,739개 중 대부분이 의원이므로 **이 데이터가 붙는 기관은 소수다.**

두 오퍼레이션의 성격이 반대다 — 섞어 쓰지 않는다:

| | 상세(Dtl) | 요약(List2) |
| --- | --- | --- |
| 한 행 | 기관 하나의 **실제 청구금액**(`curAmt`) | 기관 하나의 항목별 **가격 범위**(`minPrc`~`maxPrc`) |
| 병원별 조회 | **`ykiho` 로 된다** | **`ykiho` 가 무시된다** |
| 항목별 조회 | **안 된다**(`npayCd`·`itemCd` 무시) | `itemCd` 로 된다 |
| 분류 코드 | 없음 | 중/소/상세분류 다 있음 |
| 전건 | 259,353 | 187,627 |

**병원 상세 화면에 붙일 거라면 상세(Dtl) + `ykiho` 다** — 병원당 1콜로 그 기관의 비급여 전 항목이 나온다(예: 중앙대학교병원 666건).

가이드가 `ykiho`·`itemCd` 를 '필수'라 적어 놨지만 **둘 다 옵션이다**. 생략하면 전 기관을 페이지 단위로 받는다.

이 서비스에는 비급여항목코드정보·종별정보·지역별정보 오퍼레이션도 있지만, 여기엔 위 2종만 정제해 두었다.
 * OpenAPI spec version: 1.0.0
 */
import type { ClassCode } from './classCode';
import type { NonPaymentCode } from './nonPaymentCode';
import type { ApplyDate } from './applyDate';

/**
 * 기관 하나의 비급여 항목 한 건. **(기관, 항목) 한 쌍이 한 행**이라 `ykiho` 하나에 수백 행이 온다(중앙대학교병원 666행).

요약(List2)과 달리 가격이 범위가 아니라 **실제 청구금액 `curAmt` 단일값**이고, 대신 분류체계(중/소/상세분류) 필드가 없다.
 */
export interface NonPaymentDetailItem {
  /** 암호화된 요양기호 */
  ykiho?: string;
  /** 병원명 */
  yadmNm?: string;
  clCd?: ClassCode;
  /** 종별코드명 (예: 종합병원) */
  clCdNm?: string;
  /** 시도코드 */
  sidoCd?: number;
  /** 시도명 (예: 서울) */
  sidoCdNm?: string;
  /** 시군구코드 */
  sgguCd?: number;
  /** 시군구명 (예: 영등포구) */
  sgguCdNm?: string;
  /** 병원 비급여 안내 URL. **없는 행이 있다**(표본 4,000행 중 391건). */
  urlAddr?: string;
  /** 일련번호 */
  sno?: number;
  npayCd?: NonPaymentCode;
  /** 비급여 한글명. '대분류/중분류/소분류' 를 슬래시로 이어 붙인 형태다 (예: MRI진단료/근골격계/고관절). */
  npayKorNm?: string;
  /** 요양기관이 자체적으로 붙인 항목명 (예: 'Hip MRI', 'MegaDerm 3~4mm*1*1Cm (엘앤씨바이오)'). **표기가 기관 제각각**이라 이 값으로 기관 간 비교에 쓰지 않는다 — 비교는 `npayCd` 로 한다. */
  yadmNpayCdNm?: string;
  /** 적용개시일자 (YYYYMMDD, JSON number) */
  adtFrDd?: ApplyDate;
  /** 적용종료일자 (YYYYMMDD, JSON number). **종료가 없으면 `99991231`** 이다 — 현재 유효한 가격이라는 뜻이지 실제 날짜가 아니다. */
  adtEndDd?: ApplyDate;
  /** 현재금액 (단위: 원). 그 기관이 실제로 받는 금액이다. */
  curAmt?: number;
}
