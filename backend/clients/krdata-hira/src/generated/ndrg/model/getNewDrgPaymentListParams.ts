/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * 건강보험심사평가원(HIRA) 신포괄기준정보서비스
 * 신포괄수가제(New DRG, 묶음지불)에서 개별 분류코드(행위·약제·치료재료)가 **어떻게 취급되는지**(포괄/비포괄/전액본인부담…)를 매핑한 목록.

**코드 자체의 마스터가 아니다** — `divCd` 는 다른 서비스가 소유한 코드다(divTyCd 로 어느 체계인지 갈린다: 1=수가(mdfeeCd), 3=약가, 8=치료재료(mcatCd)). 이 서비스는 그 코드에 '신포괄에서의 포괄구분' 을 붙일 뿐이다.

**비급여 진료비(npayCd)와 무관하다.** 신포괄은 지불제도 구분이고, `ndrgItmIcsnTpCd=3(전액본인부담)·4(전액비포괄)` 가 있지만 이는 신포괄 병원 안에서의 청구 구분이지 비급여 항목 코드가 아니다.

적재·연동은 없다. 스펙·클라이언트만 만들어 둔 상태다(2026-07).
 * OpenAPI spec version: 1.0.0
 */
import type { PageNoParameter } from './pageNoParameter';
import type { NumOfRowsParameter } from './numOfRowsParameter';
import type { AdtStaDdParameter } from './adtStaDdParameter';
import type { DivTyCdParameter } from './divTyCdParameter';
import type { DivCdParameter } from './divCdParameter';
import type { NdrgItmIcsnTpCdParameter } from './ndrgItmIcsnTpCdParameter';

export type GetNewDrgPaymentListParams = {
  /**
   * 페이지 번호
   */
  pageNo?: PageNoParameter;
  /**
   * 한 페이지 결과 수. 생략하면 10.
   */
  numOfRows?: NumOfRowsParameter;
  /**
   * 적용시작일자 (YYYYMMDD). **필수다.** 20160101 로 주면 전건이 나온다.
   */
  adtStaDd: AdtStaDdParameter;
  /**
   * 분류유형코드. 어느 코드 체계인지 가른다 — 1=수가코드(mdfeeCd), 3=국산보험등재약, 8=치료재료대(mcatCd).
   */
  divTyCd?: DivTyCdParameter;
  /**
   * 분류코드 (예: H3018032). 부분일치(%A%). 이 코드의 소유·의미는 divTyCd 가 가리키는 다른 서비스에 있다.
   */
  divCd?: DivCdParameter;
  /**
   * 신포괄품목 포괄구분코드. 1=비포괄, 2=포괄, 3=전액본인부담, 4=전액비포괄, 5=행위별분리청구.
   */
  ndrgItmIcsnTpCd?: NdrgItmIcsnTpCdParameter;
};
