/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * 건강보험심사평가원(HIRA) 신포괄기준정보서비스
 * 신포괄수가제(New DRG, 묶음지불)에서 개별 분류코드(행위·약제·치료재료)가 **어떻게 취급되는지**(포괄/비포괄/전액본인부담…)를 매핑한 목록.

**코드 자체의 마스터가 아니다** — `divCd` 는 다른 서비스가 소유한 코드다(divTyCd 로 어느 체계인지 갈린다: 1=수가(mdfeeCd), 3=약가, 8=치료재료(mcatCd)). 이 서비스는 그 코드에 '신포괄에서의 포괄구분' 을 붙일 뿐이다.

**비급여 진료비(npayCd)와 무관하다.** 신포괄은 지불제도 구분이고, `ndrgItmIcsnTpCd=3(전액본인부담)·4(전액비포괄)` 가 있지만 이는 신포괄 병원 안에서의 청구 구분이지 비급여 항목 코드가 아니다.

적재·연동은 없다. 스펙·클라이언트만 만들어 둔 상태다(2026-07).
 * OpenAPI spec version: 1.0.0
 */
import type {
  GetNewDrgPaymentListParams,
  NewDrgPaymentResponse,
} from './model';

import { krDataMutator } from '../../mutator';

/**
 * 분류코드(행위·약제·치료재료)의 신포괄 포괄구분·본인부담율 인정여부를 조회한다.

**adtStaDd(적용시작일자)가 필수다.** 20160101 로 주면 전건 52,319건이 나온다(2026-07 실측). divTyCd/divCd/ndrgItmIcsnTpCd 로 좁힐 수 있다.

**가이드 응답 필드는 5개라지만 실제 10개다**(2026-07 실측) — 자기부담율 인정여부 4개(slfBrdnRtCz*)와 신포괄선별급여인정여부(ndrgSelPayAdtYn)가 가이드에 빠져 있다.
 * @summary 신포괄급여목록조회
 */
export type getNewDrgPaymentListResponse200 = {
  data: NewDrgPaymentResponse;
  status: 200;
};

export type getNewDrgPaymentListResponseSuccess =
  getNewDrgPaymentListResponse200 & {
    headers: Headers;
  };
export type getNewDrgPaymentListResponse = getNewDrgPaymentListResponseSuccess;

export const getGetNewDrgPaymentListUrl = (
  params: GetNewDrgPaymentListParams,
) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/NdrgStdInfoService/getNdrgPayList?${stringifiedParams}`
    : `/NdrgStdInfoService/getNdrgPayList`;
};

export const getNewDrgPaymentList = async (
  params: GetNewDrgPaymentListParams,
  options?: RequestInit,
): Promise<getNewDrgPaymentListResponse> => {
  return krDataMutator<getNewDrgPaymentListResponse>(
    getGetNewDrgPaymentListUrl(params),
    {
      ...options,
      method: 'GET',
    },
  );
};
