/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * 건강보험심사평가원(HIRA) 수가기준정보서비스
 * 수가코드별 적용시작일자·급여구분·상대가치점수·단가를 조회한다. 약국/한방/진료 세 종류이고 **셋의 응답 필드가 서로 다르다** — 단가 컬럼 수부터 다르니 공통 아이템으로 묶지 마라.

필드명·타입은 가이드가 아니라 실응답을 따랐다(2026-07 실측). 가이드가 틀린 곳이 있다 — 아래 각 필드 설명 참고.

**검색 파라미터를 하나도 안 주면 `totalCount: 0` 이다**(2026-07 실측). 병원목록처럼 '생략하면 전체'가 아니라, 필터가 사실상 필수인 검색형 API다. 전량을 받으려면 `mdfeeCd=%` 처럼 **와일드카드 `%`** 를 준다 — 그러면 전건이 나온다(약국 287 / 한방 10,320 / 진료 423,910건).
 * OpenAPI spec version: 1.0.0
 */
import type {
  DiagnosisMdfeeResponse,
  GetDiagnosisMdfeeListParams,
  GetOrientalMdfeeListParams,
  GetPharmacyMdfeeListParams,
  OrientalMdfeeResponse,
  PharmacyMdfeeResponse,
} from './model';

import { krDataMutator } from '../../mutator';

/**
 * 약국수가의 적용시작일자·급여구분·단가 조회. **단가 컬럼이 `unprc` 하나뿐이다** — 종별로 나뉘는 한방/진료와 다르다(약국은 종별 구분이 없다). 전건 287건으로 셋 중 가장 작다(2026-07 실측).

가이드는 이 오퍼레이션만 페이지 크기를 `numOfRow`(s 없음)라고 적어 놨는데 **틀렸다** — 실제로 `numOfRow` 는 무시되고(모르는 파라미터와 동일하게 동작해 페이지가 기본 10건이 된다) `numOfRows` 가 먹는다(2026-07 실측).
 * @summary 약국수가목록조회
 */
export type getPharmacyMdfeeListResponse200 = {
  data: PharmacyMdfeeResponse;
  status: 200;
};

export type getPharmacyMdfeeListResponseSuccess = getPharmacyMdfeeListResponse200 & {
  headers: Headers;
};
export type getPharmacyMdfeeListResponse = getPharmacyMdfeeListResponseSuccess;

export const getGetPharmacyMdfeeListUrl = (params?: GetPharmacyMdfeeListParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/mdfeeCrtrInfoService/getPharmacyMdfeeList?${stringifiedParams}`
    : `/mdfeeCrtrInfoService/getPharmacyMdfeeList`;
};

export const getPharmacyMdfeeList = async (
  params?: GetPharmacyMdfeeListParams,
  options?: RequestInit,
): Promise<getPharmacyMdfeeListResponse> => {
  return krDataMutator<getPharmacyMdfeeListResponse>(getGetPharmacyMdfeeListUrl(params), {
    ...options,
    method: 'GET',
  });
};

/**
 * 한방수가의 적용시작일자·급여구분·단가 조회. 단가가 종별 4종(한방병의원·병원·치과병의원·보건기관)으로 나뉜다. 청구기관 종별에 맞는 컬럼을 골라 써야 한다 — 해당 없는 종별은 0 이다. 전건 10,320건(2026-07 실측).
 * @summary 한방수가목록조회
 */
export type getOrientalMdfeeListResponse200 = {
  data: OrientalMdfeeResponse;
  status: 200;
};

export type getOrientalMdfeeListResponseSuccess = getOrientalMdfeeListResponse200 & {
  headers: Headers;
};
export type getOrientalMdfeeListResponse = getOrientalMdfeeListResponseSuccess;

export const getGetOrientalMdfeeListUrl = (params?: GetOrientalMdfeeListParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/mdfeeCrtrInfoService/getCmdcMdfeeList?${stringifiedParams}`
    : `/mdfeeCrtrInfoService/getCmdcMdfeeList`;
};

export const getOrientalMdfeeList = async (
  params?: GetOrientalMdfeeListParams,
  options?: RequestInit,
): Promise<getOrientalMdfeeListResponse> => {
  return krDataMutator<getOrientalMdfeeListResponse>(getGetOrientalMdfeeListUrl(params), {
    ...options,
    method: 'GET',
  });
};

/**
 * 진료수가의 적용시작일자·급여구분·단가 조회. 세 종류 중 컬럼이 가장 많고(단가 6종 + 본인부담·선별급여 인정여부) **전건 423,910건으로 모수도 압도적으로 크다**(2026-07 실측) — numOfRows=1000 이면 424콜이다. 개발계정 일 10,000 트래픽 안에는 들어가지만 한 번에 훑을 양은 아니다.

경로의 `Diagnoss` 오타는 정부포털 원본 그대로다 — 고치면 404 다.
 * @summary 진료수가목록조회
 */
export type getDiagnosisMdfeeListResponse200 = {
  data: DiagnosisMdfeeResponse;
  status: 200;
};

export type getDiagnosisMdfeeListResponseSuccess = getDiagnosisMdfeeListResponse200 & {
  headers: Headers;
};
export type getDiagnosisMdfeeListResponse = getDiagnosisMdfeeListResponseSuccess;

export const getGetDiagnosisMdfeeListUrl = (params?: GetDiagnosisMdfeeListParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/mdfeeCrtrInfoService/getDiagnossMdfeeList?${stringifiedParams}`
    : `/mdfeeCrtrInfoService/getDiagnossMdfeeList`;
};

export const getDiagnosisMdfeeList = async (
  params?: GetDiagnosisMdfeeListParams,
  options?: RequestInit,
): Promise<getDiagnosisMdfeeListResponse> => {
  return krDataMutator<getDiagnosisMdfeeListResponse>(getGetDiagnosisMdfeeListUrl(params), {
    ...options,
    method: 'GET',
  });
};
