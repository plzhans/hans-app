/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * 건강보험심사평가원(HIRA) 수가기준정보서비스
 * 수가코드별 적용시작일자·급여구분·상대가치점수·단가를 조회한다. 약국/한방/진료 세 종류이고 **셋의 응답 필드가 서로 다르다** — 단가 컬럼 수부터 다르니 공통 아이템으로 묶지 마라.

필드명·타입은 가이드가 아니라 실응답을 따랐다(2026-07 실측). 가이드가 틀린 곳이 있다 — 아래 각 필드 설명 참고.

**검색 파라미터를 하나도 안 주면 `totalCount: 0` 이다**(2026-07 실측). 병원목록처럼 '생략하면 전체'가 아니라, 필터가 사실상 필수인 검색형 API다. 전량을 받으려면 `mdfeeCd=%` 처럼 **와일드카드 `%`** 를 준다 — 그러면 전건이 나온다(약국 287 / 한방 10,320 / 진료 423,910건).
 * OpenAPI spec version: 1.0.0
 */
import type { PageNoParameter } from './pageNoParameter';
import type { NumOfRowsParameter } from './numOfRowsParameter';
import type { MdfeeCdParameter } from './mdfeeCdParameter';
import type { MdfeeDivNoParameter } from './mdfeeDivNoParameter';
import type { KorNmParameter } from './korNmParameter';

export type GetPharmacyMdfeeListParams = {
  /**
   * 페이지 번호
   */
  pageNo?: PageNoParameter;
  /**
   * 한 페이지 결과 수. 생략하면 10. 1000 까지 정상 동작한다(2026-07 실측, 전건 응답도 2초 내).
   */
  numOfRows?: NumOfRowsParameter;
  /**
   * 수가코드. **앞부분 일치(A%)** 다 — `M6561` 로 검색하면 `M6561002` 같은 하위 코드까지 70건이 걸린다(2026-07 실측). `%` 만 주면 전건이다.
   */
  mdfeeCd?: MdfeeCdParameter;
  /**
   * 수가분류번호. 앞부분 일치(A%). `%` 만 주면 전건이다.
   */
  mdfeeDivNo?: MdfeeDivNoParameter;
  /**
   * 수가 한글명. 부분 일치(%A%). `%` 만 주면 전건이다.
   */
  korNm?: KorNmParameter;
};
