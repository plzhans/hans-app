/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * 건강보험심사평가원(HIRA) 병원평가정보서비스
 * OpenAPI spec version: 1.0.0
 */
import type { HospitalAssessmentItemClCd } from './hospitalAssessmentItemClCd';
import type { AssessmentGrade } from './assessmentGrade';
import type { HospitalAssessmentItemAsmGrd16 } from './hospitalAssessmentItemAsmGrd16';

/**
 * 기관 하나의 평가등급. 필드는 가이드가 아니라 실응답을 따랐다(2026-07 실측, 표본 1,000건).

**asmGrd02·asmGrd11 은 존재하지 않는다** — 원본에 없다(폐지 항목으로 보인다). 번호가 연속이 아니다.

평가대상은 36,599개 기관이고 대부분 의원(clCd=31)이다. 병원급 전용이 아니다.
 */
export interface HospitalAssessmentItem {
  /** 암호화된 요양기호 */
  ykiho?: string;
  /** 요양기관명 */
  yadmNm?: string;
  /** 종별코드. **숫자와 문자열이 섞여 온다**(2026-07 실측). '01'(상급종합)만 문자열이고 나머지(11·21·28·29·31·71·72·75…)는 정수다 — 앞자리 0 은 JSON 숫자가 될 수 없어 문자열로 남는다. 비교할 땐 asString() 으로 맞춰라. */
  clCd?: HospitalAssessmentItemClCd;
  /** 종별코드명 */
  clCdNm?: string;
  /** 주소 */
  addr?: string;
  /** 급성기뇌졸중 */
  asmGrd01?: AssessmentGrade;
  /** 혈액투석 */
  asmGrd03?: AssessmentGrade;
  /** 의료급여정신과 */
  asmGrd04?: AssessmentGrade;
  /** 수술부위 감염예방 항생제 */
  asmGrd05?: AssessmentGrade;
  /** 관상동맥우회술 */
  asmGrd06?: AssessmentGrade;
  /** 급성상기도감염 항생제 처방률 */
  asmGrd07?: AssessmentGrade;
  /** 주사제 처방률 */
  asmGrd08?: AssessmentGrade;
  /** 약품목수 */
  asmGrd09?: AssessmentGrade;
  /** 요양병원 */
  asmGrd10?: AssessmentGrade;
  /** 대장암 */
  asmGrd12?: AssessmentGrade;
  /** 위암 */
  asmGrd13?: AssessmentGrade;
  /** 유방암 */
  asmGrd14?: AssessmentGrade;
  /** 폐암 */
  asmGrd15?: AssessmentGrade;
  /** 천식. **이 항목만 값 체계가 다르다**(2026-07 실측). 다른 항목이 1~5 와 '등급제외' 를 쓰는 것과 달리 0·2·3·4·5 와 '양호' 가 온다 — 1 도 '등급제외' 도 없다. 가이드의 '양호한 기관 공개' 와 맞물리지만 0 과 2~5 의 의미는 확인되지 않았다. **1~5 등급으로 가정하지 마라.** */
  asmGrd16?: HospitalAssessmentItemAsmGrd16;
  /** 만성폐쇄성폐질환 */
  asmGrd17?: AssessmentGrade;
  /** 폐렴 */
  asmGrd18?: AssessmentGrade;
  /** 중환자실 */
  asmGrd19?: AssessmentGrade;
  /** 신생아중환자실 */
  asmGrd20?: AssessmentGrade;
  /** 마취 */
  asmGrd21?: AssessmentGrade;
  /** 정신건강 입원영역 */
  asmGrd22?: AssessmentGrade;
  /** 급성하기도감염 항생제 처방률 */
  asmGrd23?: AssessmentGrade;
  /** 고혈압·당뇨병 */
  asmGrd24?: AssessmentGrade;
}
