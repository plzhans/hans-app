/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * 건강보험심사평가원(HIRA) 치료재료정보조회서비스
 * 치료재료(의료기기·소모품)의 급여/비급여 목록. 스텐트·페이스메이커·리드선 같은 **물리적 재료**다.

**비급여 진료비(nonPaymentDamtInfoService)와 다른 축이다 — 섞지 마라.**
| | 이 서비스(치료재료) | 비급여 진료비 |
| --- | --- | --- |
| 대상 | 물리적 재료(리드선·스텐트) | 진료행위·수수료(진단서·초음파) |
| 코드 | `mcatCd`(G8207001) | `npayCd`(ABZ010001) |
| 가격 | 전국 공통 **상한단가**(mxUnprc) | 기관별 **실제 청구액** |
| 급여구분 | 재료 자체의 급여/비급여 | 기관이 받는 비급여 |

적재·연동은 아직 없다. 스펙과 클라이언트만 만들어 둔 상태다(2026-07).
 * OpenAPI spec version: 1.0.0
 */
import type { GetMaterialListParams, MaterialListResponse } from './model';

import { krDataMutator } from '../../mutator';

/**
 * 치료재료코드·품목명·업체명·중분류코드로 치료재료의 급여구분·분류·상한단가를 조회한다.

**검색 조건이 하나도 없으면 0건이다**(2026-07 실측 — totalCount=0, items='' 로 온다). 전건 훑기가 아니라 조건 조회용이다. mcatCd/itmNm/mnfEntpNm/impEntpNm/mdivCd 중 하나는 줘라.

**급여구분(payTpNm)은 급여/비급여 이진이 아니다** — 급여·급여중지·삭제가 섞여 온다(2026-07 실측). 비급여만 필요하면 호출부에서 걸러라.
 * @summary 치료재료 급여비급여 목록조회
 */
export type getMaterialListResponse200 = {
  data: MaterialListResponse;
  status: 200;
};

export type getMaterialListResponseSuccess = getMaterialListResponse200 & {
  headers: Headers;
};
export type getMaterialListResponse = getMaterialListResponseSuccess;

export const getGetMaterialListUrl = (params?: GetMaterialListParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/mcatInfoService1.2/getPaymentNonPaymentList1.2?${stringifiedParams}`
    : `/mcatInfoService1.2/getPaymentNonPaymentList1.2`;
};

export const getMaterialList = async (
  params?: GetMaterialListParams,
  options?: RequestInit,
): Promise<getMaterialListResponse> => {
  return krDataMutator<getMaterialListResponse>(getGetMaterialListUrl(params), {
    ...options,
    method: 'GET',
  });
};
