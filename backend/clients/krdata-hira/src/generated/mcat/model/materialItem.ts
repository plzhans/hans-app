/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * 건강보험심사평가원(HIRA) 치료재료정보조회서비스
 * 치료재료(의료기기·소모품)의 급여/비급여 목록. 스텐트·페이스메이커·리드선 같은 **물리적 재료**다.

**비급여 진료비(nonPaymentDamtInfoService)와 다른 축이다 — 섞지 마라.**
| | 이 서비스(치료재료) | 비급여 진료비 |
| --- | --- | --- |
| 대상 | 물리적 재료(리드선·스텐트) | 진료행위·수수료(진단서·초음파) |
| 코드 | `mcatCd`(G8207001) | `npayCd`(ABZ010001) |
| 가격 | 전국 공통 **상한단가**(mxUnprc) | 기관별 **실제 청구액** |
| 급여구분 | 재료 자체의 급여/비급여 | 기관이 받는 비급여 |

적재·연동은 아직 없다. 스펙과 클라이언트만 만들어 둔 상태다(2026-07).
 * OpenAPI spec version: 1.0.0
 */

/**
 * 치료재료 한 건. 재료 하나의 분류·상한단가·급여구분이다. **기관과 무관하다** — 전국 공통 마스터다.
 */
export interface MaterialItem {
  /** 치료재료(재료대)코드 (예: G8207001). 이 마스터의 식별자다. */
  mcatCd?: string;
  /** 품목명 (예: SENTUS OTW BP) */
  itmNm?: string;
  /** 규격명 (예: 전규격) */
  nomNm?: string;
  /** 단위 (예: 1EA) */
  unit?: string;
  /** 상한단가(원). **가이드는 string 이라지만 JSON number 로 온다**(2026-07 실측). 기관별 청구액이 아니라 전국 공통 상한이다. */
  mxUnprc?: number;
  /** 급여구분명. **급여/비급여 이진이 아니다** — 급여·급여중지·삭제가 섞여 온다(2026-07 실측). */
  payTpNm?: string;
  /** 대분류군코드 (예: G) */
  ldgrpCd?: string;
  /** 대분류군코드명 (예: 흉부외과용 군) */
  ldgrpCdNm?: string;
  /** 대분류코드 (예: G8) */
  ldivCd?: string;
  /** 대분류코드명 (예: PACEMAKER, CRT, ICD & LEAD 류) */
  ldivCdNm?: string;
  /** 중분류코드 (예: 078011) */
  mdivCd?: string;
  /** 중분류코드명 (예: CRT용 LV LEAD(4극 미만)) */
  mdivCdNm?: string;
  /** 제조업체명 (예: BIOTRONIK SE&CO.KG) */
  mnfEntpNm?: string;
  /** 수입업체명 (예: 바이오트로닉코리아). 국산이면 없을 수 있다. */
  impEntpNm?: string;
}
