{
  "name": "@hansapp/mcp",
  "version": "0.0.1",
  "private": true,
  "description": "MCP 서버. 외부 AI 클라이언트에 우리 도구를 노출한다. LLM 을 부르지 않는다 — 추론은 붙은 쪽 모델이 한다.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "src": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "dependencies": {
    "@hansapp/application": "workspace:*",
    "@hansapp/common": "workspace:*",
    "@modelcontextprotocol/express": "^2.0.0",
    "@modelcontextprotocol/node": "^2.0.0",
    "@modelcontextprotocol/server": "^2.0.0",
    "@nestjs/common": "^11.0.1",
    "reflect-metadata": "^0.2.2",
    "zod": "^4.4.3"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@types/node": "^24.13.2"
  }
}
