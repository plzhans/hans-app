/**
 * Generated by orval v8.20.0 🍺
 * Do not edit manually.
 * Hans API
 * Hans API backend 문서
 * OpenAPI spec version: 0.17.0
 */
import type {
  HiraAddressCodeResponse,
  HiraCodeControllerListParams,
  HiraEquipmentCodeResponse,
  HiraHospitalAssessmentResponse,
  HiraHospitalControllerGetNonPaymentsParams,
  HiraHospitalControllerListParams,
  HiraHospitalListResponse,
  HiraInstitutionClassCodeResponse,
  HiraNonPaymentDetailResponse,
  HiraRegionControllerListRegionsParams,
  HiraRegionResponseDto,
  HiraSearchCodeResponse,
  HiraSubjectCodeResponse,
  HiraSubjectInfoResponse
} from '../../model';

import { hansApiFetch } from '../../../http';

export const getHiraHospitalControllerListUrl = (params?: HiraHospitalControllerListParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/data-go-kr/hira/hospitals?${stringifiedParams}` : `/data-go-kr/hira/hospitals`
}

/**
 * 로컬 DB 에 미러링한 HIRA 병원 목록. 응답 구조는 원본 API 와 동일하다.
 * @summary 병원 목록 조회
 */
export const hiraHospitalControllerList = async (params?: HiraHospitalControllerListParams, options?: RequestInit): Promise<HiraHospitalListResponse> => {

  return hansApiFetch<HiraHospitalListResponse>(getHiraHospitalControllerListUrl(params),
  {
    ...options,
    method: 'GET'


  }
);}


export const getHiraHospitalControllerGetUrl = (ykiho: string,) => {




  return `/data-go-kr/hira/hospitals/${ykiho}`
}

/**
 * 암호화된 요양기호로 1건 조회. 없으면 items 가 빈 배열이고 totalCount 가 0 이다.
 * @summary 병원 상세 조회
 */
export const hiraHospitalControllerGet = async (ykiho: string, options?: RequestInit): Promise<HiraHospitalListResponse> => {

  return hansApiFetch<HiraHospitalListResponse>(getHiraHospitalControllerGetUrl(ykiho),
  {
    ...options,
    method: 'GET'


  }
);}


export const getHiraHospitalControllerGetNonPaymentsUrl = (ykiho: string,
    params?: HiraHospitalControllerGetNonPaymentsParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/data-go-kr/hira/hospitals/${ykiho}/npay?${stringifiedParams}` : `/data-go-kr/hira/hospitals/${ykiho}/npay`
}

/**
 * 기관이 신고한 비급여 항목별 실제 청구금액(curAmt). 응답 구조는 원본 API 와 동일하다.
 *
 * **병원급 이상만 있다** — 의원(clCd=31)은 원본에 통째로 없어 늘 빈 배열이다. **한 기관에 수백 행이다**(최다 1,048건) — 페이지로 받아라.
 * @summary 비급여 진료비 조회
 */
export const hiraHospitalControllerGetNonPayments = async (ykiho: string,
    params?: HiraHospitalControllerGetNonPaymentsParams, options?: RequestInit): Promise<HiraNonPaymentDetailResponse> => {

  return hansApiFetch<HiraNonPaymentDetailResponse>(getHiraHospitalControllerGetNonPaymentsUrl(ykiho,params),
  {
    ...options,
    method: 'GET'


  }
);}


export const getHiraHospitalControllerGetAssessmentUrl = (ykiho: string,) => {




  return `/data-go-kr/hira/hospitals/${ykiho}/asm`
}

/**
 * 기관이 받은 평가항목별 등급. 응답 구조는 원본 API 와 동일하다.
 *
 * **한 기관에 1건이다** — 평가항목이 item 에 asmGrd01~24 로 가로로 붙는다(02·11 은 원본에 없다). 항목 번호의 이름은 /data-go-kr/hira/codes 가 아니라 통합 병원 상세가 붙여준다.
 *
 * **평가대상이 아니면 items 가 빈 배열이다** — 평가대상은 36,599곳으로 병원 전체의 부분집합이다. 대부분 의원(clCd=31)이라 병원급 전용은 아니다.
 *
 * **등급 값은 원본 그대로다.** 정수 1~5 와 문자열 `등급제외`(평가는 했으나 등급 미부여)가 섞여 온다. 키 자체가 없으면 평가대상 제외라는 뜻이라 `등급제외` 와 다르다 — 합치면 정보가 사라진다. 천식(asmGrd16)만 인코딩이 달라 1 대신 `양호`, 등급제외 대신 0 을 쓴다. 0 은 최하가 아니라 등급제외다.
 * @summary 병원평가 등급 조회
 */
export const hiraHospitalControllerGetAssessment = async (ykiho: string, options?: RequestInit): Promise<HiraHospitalAssessmentResponse> => {

  return hansApiFetch<HiraHospitalAssessmentResponse>(getHiraHospitalControllerGetAssessmentUrl(ykiho),
  {
    ...options,
    method: 'GET'


  }
);}


export const getHiraCodeControllerListUrl = (params: HiraCodeControllerListParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/data-go-kr/hira/codes?${stringifiedParams}` : `/data-go-kr/hira/codes`
}

/**
 * 로컬 DB 에 미러링한 HIRA 코드. tp 로 종류를 고른다. 응답 item 의 필드명은 종류별 원본 API 와 같다.
 * @summary 코드 목록 조회
 */
export const hiraCodeControllerList = async (params: HiraCodeControllerListParams, options?: RequestInit): Promise<HiraAddressCodeResponse | HiraInstitutionClassCodeResponse | HiraSubjectCodeResponse | HiraEquipmentCodeResponse | HiraSearchCodeResponse> => {

  return hansApiFetch<HiraAddressCodeResponse | HiraInstitutionClassCodeResponse | HiraSubjectCodeResponse | HiraEquipmentCodeResponse | HiraSearchCodeResponse>(getHiraCodeControllerListUrl(params),
  {
    ...options,
    method: 'GET'


  }
);}


export const getHiraRegionControllerListRegionsUrl = (params?: HiraRegionControllerListRegionsParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/data-go-kr/hira/regions?${stringifiedParams}` : `/data-go-kr/hira/regions`
}

/**
 * 병원 데이터에서 집계한 시도·시군구 목록(코드 포함). 병원이 1건 이상 있는 지역만 나온다.
 * @summary 지역 목록 조회
 */
export const hiraRegionControllerListRegions = async (params?: HiraRegionControllerListRegionsParams, options?: RequestInit): Promise<HiraRegionResponseDto> => {

  return hansApiFetch<HiraRegionResponseDto>(getHiraRegionControllerListRegionsUrl(params),
  {
    ...options,
    method: 'GET'


  }
);}


export const getHiraRegionControllerListSubjectsUrl = (ykiho: string,) => {




  return `/data-go-kr/hira/hospitals/${ykiho}/subjects`
}

/**
 * 해당 병원이 진료하는 과목 목록. 응답 구조는 원본 API(진료과목정보)와 동일하다.
 * @summary 병원 진료과목 조회
 */
export const hiraRegionControllerListSubjects = async (ykiho: string, options?: RequestInit): Promise<HiraSubjectInfoResponse> => {

  return hansApiFetch<HiraSubjectInfoResponse>(getHiraRegionControllerListSubjectsUrl(ykiho),
  {
    ...options,
    method: 'GET'


  }
);}


