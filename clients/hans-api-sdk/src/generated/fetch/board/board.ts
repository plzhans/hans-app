/**
 * Generated by orval v8.20.0 🍺
 * Do not edit manually.
 * Hans API
 * Hans API backend 문서
 * OpenAPI spec version: 0.17.0
 */
import type {
  BoardControllerListPosts200,
  BoardControllerListPostsParams,
  PublicBoardDto,
  PublicPostDetailDto
} from '../../model';

import { hansApiFetch } from '../../../http';

export const getBoardControllerListUrl = () => {




  return `/boards`
}

/**
 * 공개(ACTIVE)된 게시판만. 포털 메뉴가 이걸로 그려진다.
 *
 * `Cache-Control: public, max-age=60` 이 붙는다 — 인자도 로그인도 없어 누구에게나 같은 응답이고, 포털이 화면마다 부르기 때문이다. 게시판을 새로 만들면 최대 1분 뒤에 메뉴에 나타난다.
 * @summary 게시판 목록
 */
export const boardControllerList = async ( options?: RequestInit): Promise<PublicBoardDto[]> => {

  return hansApiFetch<PublicBoardDto[]>(getBoardControllerListUrl(),
  {
    ...options,
    method: 'GET'


  }
);}


export const getBoardControllerListPostsUrl = (name: string,
    params?: BoardControllerListPostsParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/boards/${name}/posts?${stringifiedParams}` : `/boards/${name}/posts`
}

/**
 * 공개된 글만. 고정 글이 먼저, 그다음 최신순.
 *
 * **인자 없이 부른 첫 페이지에만** `Cache-Control: public, max-age=60` 이 붙는다 — 게시판을 열면 누구나 보게 되는 한 장이라, 링크가 퍼질 때의 순간 트래픽을 CDN 이 받아 낸다. 페이지를 넘기거나 크기를 바꾸면 no-store 다. 새 글은 최대 1분 뒤에 목록에 나타난다.
 * @summary 게시글 목록
 */
export const boardControllerListPosts = async (name: string,
    params?: BoardControllerListPostsParams, options?: RequestInit): Promise<BoardControllerListPosts200> => {

  return hansApiFetch<BoardControllerListPosts200>(getBoardControllerListPostsUrl(name,params),
  {
    ...options,
    method: 'GET'


  }
);}


export const getBoardControllerGetPostUrl = (name: string,
    id: number,) => {




  return `/boards/${name}/posts/${id}`
}

/**
 * 비공개 글의 본문은 쓴 사람에게만 나간다 — 나머지에게는 content 가 빠진다.
 *
 * **공개 글에만** `Cache-Control: public, max-age=60` 이 붙는다 — 오래 태우려는 것이 아니라 링크가 퍼질 때의 순간 트래픽을 CDN 이 받아 내기 위한 1분이다. 그동안의 조회는 서버에 닿지 않아 조회수도 그만큼 덜 는다. 비공개 글은 no-store 라 공유 캐시에 남지 않는다.
 * @summary 게시글 하나
 */
export const boardControllerGetPost = async (name: string,
    id: number, options?: RequestInit): Promise<PublicPostDetailDto> => {

  return hansApiFetch<PublicPostDetailDto>(getBoardControllerGetPostUrl(name,id),
  {
    ...options,
    method: 'GET'


  }
);}


