/**
 * Generated by orval v8.20.0 🍺
 * Do not edit manually.
 * Hans API
 * Hans API backend 문서
 * OpenAPI spec version: 0.17.0
 */
import type {
  AddressControllerSearchEnglish200,
  AddressControllerSearchEnglishParams,
  RegionControllerList200,
  RegionControllerListParams,
  RegionControllerReverseParams,
  RegionPointDto
} from '../../model';

import { hansApiFetch } from '../../../http';

export const getRegionControllerListUrl = (params?: RegionControllerListParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/address/regions?${stringifiedParams}` : `/address/regions`
}

/**
 * 시도 → 시군구 2단계다.
 *
 * `level=sido` 로 시도를, `level=sggu&parent=11` 로 그 시도의 시군구를 받는다.
 *
 * 병원 검색의 `region` 파라미터에 시군구 코드를 넘긴다.
 * @summary 지역 코드
 */
export const regionControllerList = async (params?: RegionControllerListParams, options?: RequestInit): Promise<RegionControllerList200> => {

  return hansApiFetch<RegionControllerList200>(getRegionControllerListUrl(params),
  {
    ...options,
    method: 'GET'


  }
);}


export const getRegionControllerReverseUrl = (params: RegionControllerReverseParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/address/regions/reverse?${stringifiedParams}` : `/address/regions/reverse`
}

/**
 * 위경도를 주면 그 좌표가 속한 시도·시군구를 돌려준다. "내 위치" 버튼이 브라우저에서 받은 좌표를 지역 필터로 바꿀 때 쓴다.
 *
 * 받은 코드는 **병원 검색의 `region` 파라미터에 그대로** 넣으면 된다 — `region` 이 있으면 그 시군구 코드를, 없으면 `sido` 코드를 보낸다.
 *
 * **한국 밖이거나 주변에 병원이 없으면 404** 다. 위치를 못 알아낸 것이지 오류가 아니니, 클라이언트는 조용히 지역 선택을 비워두면 된다.
 * @summary 좌표로 지역 코드 조회
 */
export const regionControllerReverse = async (params: RegionControllerReverseParams, options?: RequestInit): Promise<RegionPointDto> => {

  return hansApiFetch<RegionPointDto>(getRegionControllerReverseUrl(params),
  {
    ...options,
    method: 'GET'


  }
);}


export const getAddressControllerSearchEnglishUrl = (params: AddressControllerSearchEnglishParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/address/english?${stringifiedParams}` : `/address/english`
}

/**
 * 한글 주소(또는 일부)를 검색해 매칭되는 주소를 **영문 표기**와 함께 반환한다.
 *
 * **시/도명 단독·한 글자·숫자만으로는 검색되지 않는다**(도로명주소 API 제약). 결과가 없으면 빈 목록을 반환한다.
 * @summary 영문 주소 검색
 */
export const addressControllerSearchEnglish = async (params: AddressControllerSearchEnglishParams, options?: RequestInit): Promise<AddressControllerSearchEnglish200> => {

  return hansApiFetch<AddressControllerSearchEnglish200>(getAddressControllerSearchEnglishUrl(params),
  {
    ...options,
    method: 'GET'


  }
);}


