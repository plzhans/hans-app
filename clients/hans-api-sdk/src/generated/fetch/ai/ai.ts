/**
 * Generated by orval v8.20.0 🍺
 * Do not edit manually.
 * Hans API
 * Hans API backend 문서
 * OpenAPI spec version: 0.17.0
 */
import type {
  AiSearchRequestDto,
  AiSearchResponseDto,
  CapabilitiesResponseDto
} from '../../model';

import { hansApiFetch } from '../../../http';

export const getHealthcareAiSearchControllerSearchUrl = () => {




  return `/healthcare/ai-search`
}

/**
 * 자연어 질문을 검색 조건으로 변환한다. **병원 목록은 반환하지 않는다** — 응답의 `tool` 로 동작을 고르고 `params` 를 실어 `GET /healthcare/hospitals` 를 호출한다.
 *
 * 좌표는 주고받지 않는다. 측위는 클라이언트가 하고, 서버는 위치 기준으로 검색할지 여부만 정한다.
 *
 * 진단·치료 조언은 하지 않는다. 병원을 고르기 위한 조건만 만든다.
 * @summary 자연어 병원 검색
 */
export const healthcareAiSearchControllerSearch = async (aiSearchRequestDto: AiSearchRequestDto, options?: RequestInit): Promise<AiSearchResponseDto> => {

  return hansApiFetch<AiSearchResponseDto>(getHealthcareAiSearchControllerSearchUrl(),
  {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(aiSearchRequestDto)
  }
);}


export const getAiCapabilitiesControllerCapabilitiesUrl = () => {




  return `/ai/capabilities`
}

/**
 * 남은 사용량과 선택할 수 있는 모델을 반환한다. **사용량을 소모하지 않는다.**
 *
 * AI 기능을 **열 때 한 번** 호출한다. 이후 값은 검색 응답(`quota`)에 실려 오므로 주기적으로 호출하지 않는다.
 *
 * 누구의 사용량인지는 자격증명이 정한다 — access token 이면 사용자, 클라이언트 키면 해당 앱의 몫이다.
 *
 * `quota` 가 비어 오면 걸린 한도가 없다는 뜻이다. 사용량을 확인할 수 없는 상태면 **503** 이며, 이때는 검색도 거절되므로 AI 기능을 비활성화하는 것이 맞다.
 * @summary AI 사용량 · 모델 목록
 */
export const aiCapabilitiesControllerCapabilities = async ( options?: RequestInit): Promise<CapabilitiesResponseDto> => {

  return hansApiFetch<CapabilitiesResponseDto>(getAiCapabilitiesControllerCapabilitiesUrl(),
  {
    ...options,
    method: 'GET'


  }
);}


