/**
 * Generated by orval v8.20.0 🍺
 * Do not edit manually.
 * Hans API
 * Hans API backend 문서
 * OpenAPI spec version: 0.17.0
 */
import type {
  HealthcareHospitalControllerNearbyParams,
  HealthcareHospitalControllerScroll200,
  HealthcareHospitalControllerScrollParams,
  HealthcareHospitalControllerSearch200,
  HealthcareHospitalControllerSearchParams,
  HospitalDetailDto,
  HospitalNearbyResponseDto,
  HospitalNonPaymentDto,
  NonPaymentRequestResultDto
} from '../../model';

import { hansApiFetch } from '../../../http';

export const getHealthcareHospitalControllerSearchUrl = (params?: HealthcareHospitalControllerSearchParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/healthcare/hospitals?${stringifiedParams}` : `/healthcare/hospitals`
}

/**
 * 지역·종별·진료과목·병원명으로 검색한다. 응급실 운영, 달빛어린이병원 필터도 있다.
 * @summary 병원 검색
 */
export const healthcareHospitalControllerSearch = async (params?: HealthcareHospitalControllerSearchParams, options?: RequestInit): Promise<HealthcareHospitalControllerSearch200> => {

  return hansApiFetch<HealthcareHospitalControllerSearch200>(getHealthcareHospitalControllerSearchUrl(params),
  {
    ...options,
    method: 'GET'


  }
);}


export const getHealthcareHospitalControllerScrollUrl = (params?: HealthcareHospitalControllerScrollParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/healthcare/hospitals/scroll?${stringifiedParams}` : `/healthcare/hospitals/scroll`
}

/**
 * 검색(GET /healthcare/hospitals)과 **필터는 같고 페이징 방식만 다르다.** 페이지 번호 대신 nextToken 으로 이어 받는다 — 무한 스크롤 화면용이다.
 *
 * **첫 호출은 nextToken 없이** 필터만 보낸다. 응답의 nextToken 을 다음 호출에 그대로 실어 보내면 이어진다. **nextToken 이 없으면 마지막 페이지다** — 그만 부른다.
 * @summary 병원 무한 스크롤
 */
export const healthcareHospitalControllerScroll = async (params?: HealthcareHospitalControllerScrollParams, options?: RequestInit): Promise<HealthcareHospitalControllerScroll200> => {

  return hansApiFetch<HealthcareHospitalControllerScroll200>(getHealthcareHospitalControllerScrollUrl(params),
  {
    ...options,
    method: 'GET'


  }
);}


export const getHealthcareHospitalControllerGetUrl = (id: number,) => {




  return `/healthcare/hospitals/${id}`
}

/**
 * 진료과목·진료시간·인력·병상·장비·역량을 함께 반환한다. 진료시간은 kind 로 갈린다 — general(일반)과 baby(달빛어린이)는 시간대가 다르다.
 * @summary 병원 상세
 */
export const healthcareHospitalControllerGet = async (id: number, options?: RequestInit): Promise<HospitalDetailDto> => {

  return hansApiFetch<HospitalDetailDto>(getHealthcareHospitalControllerGetUrl(id),
  {
    ...options,
    method: 'GET'


  }
);}


export const getHealthcareHospitalControllerNearbyUrl = (id: number,
    params?: HealthcareHospitalControllerNearbyParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/healthcare/hospitals/${id}/nearby?${stringifiedParams}` : `/healthcare/hospitals/${id}/nearby`
}

/**
 * 이 병원을 **대체할 만한** 근처 병원을 찾는다.
 *
 * **단순히 가까운 순이 아니다.** 진료과목이 겹치는지를 가장 크게 보고, 전문병원 지정분야·종별·응급실 운영 여부로 보정한 뒤, 거리를 가중치로 곱해 정렬한다.
 *
 * 겹친 과목은 **matchedSubjects** 로 함께 반환하므로, 결과에 포함된 근거를 화면에 표시할 수 있다.
 *
 * 요양병원·정신병원은 기본적으로 제외되며, **기준 병원이 그 계열이면 같은 계열에서만** 찾는다.
 *
 * 결과가 비어도 정상이다(좌표가 없는 병원이거나 반경 안에 후보가 없다). 병원 자체가 없을 때만 404 다.
 * @summary 근처의 유사한 병원
 */
export const healthcareHospitalControllerNearby = async (id: number,
    params?: HealthcareHospitalControllerNearbyParams, options?: RequestInit): Promise<HospitalNearbyResponseDto> => {

  return hansApiFetch<HospitalNearbyResponseDto>(getHealthcareHospitalControllerNearbyUrl(id,params),
  {
    ...options,
    method: 'GET'


  }
);}


export const getHealthcareHospitalControllerNonPaymentsUrl = (id: number,) => {




  return `/healthcare/hospitals/${id}/hira-npay`
}

/**
 * 병원이 신고한 비급여 항목별 가격. 대분류 → 표준코드로 묶어 **기관 전건을 한 번에** 돌려준다(페이지 없음).
 *
 * **빈 categories 도 정상이다.** 비급여를 신고한 기관은 전체의 약 4%(3,511곳)이며, 의원급(clCd=31)은 원본에 없어 항상 비어 있다. 404 는 병원 자체가 없을 때만 반환한다.
 *
 * **금액은 범위다.** 한 표준코드에 원본 행이 여럿일 수 있어서다(체외충격파가 단순/복잡 두 행). 단일가면 minAmount 와 maxAmount 가 같으며, 이때는 범위로 표시하지 않는다.
 *
 * 한 기관에 수백 행이 될 수 있어(최다 1,048행) 병원 상세와 분리된 엔드포인트다.
 * @summary 비급여 진료비
 */
export const healthcareHospitalControllerNonPayments = async (id: number, options?: RequestInit): Promise<HospitalNonPaymentDto> => {

  return hansApiFetch<HospitalNonPaymentDto>(getHealthcareHospitalControllerNonPaymentsUrl(id),
  {
    ...options,
    method: 'GET'


  }
);}


export const getHealthcareHospitalControllerRequestNonPaymentsUrl = (id: number,) => {




  return `/healthcare/hospitals/${id}/hira-npay/request`
}

/**
 * 이 병원의 비급여를 받아오도록 **요청만 한다.** 즉시 반영되지 않는다 — 큐에 등록되고 배치가 처리한다.
 *
 * **`source=requestable` 일 때만 의미가 있다.** 공개 API 에 이미 있으면(`hira`) 요청할 이유가 없고, 받아봤는데 없으면(`none`) 다시 요청해도 결과가 같다.
 *
 * **같은 병원을 여러 번 눌러도 큐에는 한 줄이다.** 처리 결과는 다음 조회의 `source` 로 나타난다(web|none).
 * @summary 비급여 갱신 요청
 */
export const healthcareHospitalControllerRequestNonPayments = async (id: number, options?: RequestInit): Promise<NonPaymentRequestResultDto> => {

  return hansApiFetch<NonPaymentRequestResultDto>(getHealthcareHospitalControllerRequestNonPaymentsUrl(id),
  {
    ...options,
    method: 'POST'


  }
);}


