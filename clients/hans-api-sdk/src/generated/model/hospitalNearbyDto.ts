/**
 * Generated by orval v8.20.0 🍺
 * Do not edit manually.
 * Hans API
 * Hans API backend 문서
 * OpenAPI spec version: 0.17.0
 */
import type { CodeDto } from './codeDto';
import type { LocationDto } from './locationDto';
import type { MatchedSubjectDto } from './matchedSubjectDto';

export interface HospitalNearbyDto {
  /** 통합 병원 id */
  id: number;
  name: string;
  /** 종별. 의원·종합병원·치과의원 … */
  category?: CodeDto;
  /**
     * 병원 등급. TIER1(의원급) | TIER2(병원급) | TIER3(상급종합) | NURSING | MENTAL.
     * 종별에서 유도한 값이다 — 의료법이 병상 수로 종별을 규정하므로 등급이 종별에 이미 들어 있다.
     */
  tier?: CodeDto;
  /** 전문병원 지정분야(관절·척추·심장 …). 보건복지부 지정이라 병원당 최대 1건이다. */
  specialty?: CodeDto;
  location: LocationDto;
  tel?: string;
  /** 응급실 운영 */
  emergency: boolean;
  /** 달빛어린이병원 (야간·휴일 소아진료) */
  baby: boolean;
  /**
     * 이 병원 정보가 마지막으로 바뀐 시각.
     *
     * 원본은 수정 시각을 제공하지 않는다. 수집한 내용을 직전 회차와 대조해 달라진 병원에만 기록하므로, **원본이 수정된 시각이 아니라 변경이 확인된 시각**이며 수집 주기만큼 늦다. 진료과목·진료시간 등 하위 정보의 변경도 이 값에 반영된다.
     */
  updatedAt: string;
  /** 기준 병원으로부터의 **직선거리(m)**. 도로 거리도 소요시간도 아니다 — "420m" 처럼 대략의 가까움을 보여주는 용도다. */
  distance?: number;
  /**
     * 기준 병원과 겹친 진료과목. 이 병원이 결과에 포함된 근거다.
     *
     * **빈 배열일 수 있다** — 겹치는 과목이 없어도 반경 안이면 거리순으로 채운다.
     */
  matchedSubjects: MatchedSubjectDto[];
}
